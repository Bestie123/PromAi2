# 🚀 Быстрый старт

## Просмотр вариантов

1. Откройте `index.html` в браузере
2. Выберите один из 4 вариантов раскладки
3. Используйте колесо мыши для зума, ЛКМ для перемещения

## Регенерация координат

```bash
node coordinate_generator.js
```

Создаст 4 варианта:
- ✅ `nodes_grid.json` - сетка 5×6
- ✅ `nodes_tree.json` - иерархическое дерево
- ✅ `nodes_circular.json` - круговая раскладка
- ✅ `nodes_force.json` - физическая симуляция

## Запуск локального сервера

### Windows (PowerShell)
```powershell
.\server_simple.ps1
```

### Python
```bash
python server.py
```

Затем откройте: http://localhost:3000

## Что уже работает

✅ Автогенерация координат для 29 критериев  
✅ 4 варианта раскладки  
✅ Интерактивная визуализация  
✅ Зум и панорамирование  
✅ Связи между узлами  
✅ Цветовая кодировка типов решений  

## Структура файлов

```
architecture/
├── nodes.json              # Исходные данные
├── coordinate_generator.js # Генератор
├── nodes_*.json           # Варианты раскладки
├── viewer_*.html          # Просмотрщики
├── index.html             # Главная страница
└── README.md              # Полная документация
```

## Добавление своего варианта

1. Откройте `coordinate_generator.js`
2. Добавьте функцию `generateMyLayout(nodes)`
3. Зарегистрируйте в `LAYOUTS`
4. Запустите `node coordinate_generator.js`
5. Создайте `viewer_my.html`
6. Добавьте карточку в `index.html`

## Параметры раскладки

Измените в `coordinate_generator.js`:

```javascript
const PARAMS = {
  nodeWidth: 320,
  nodeHeight: 200,
  horizontalGap: 400,
  verticalGap: 300,
  canvasWidth: 12000,
  canvasHeight: 8000
};
```

## Типы узлов

- 🔵 `question` - вопрос
- 🔴 `critical` - критичное решение
- 🟠 `important` - важное решение
- 🟢 `flexible` - гибкое решение
- ⚪ `rejected` - отклонённое решение

## Горячие клавиши

- **Колесо мыши** - зум in/out
- **ЛКМ + drag** - перемещение canvas
- **Кнопка "Сброс"** - вернуть начальный вид
- **Кнопка "Центр"** - центрировать canvas

## Проблемы?

1. **JSON не загружается** - запустите локальный сервер
2. **Узлы наложились** - измените параметры в генераторе
3. **Медленно работает** - уменьшите количество узлов или упростите связи

## Следующие шаги

- [ ] Добавить фильтры по типам узлов
- [ ] Экспорт в PNG/SVG
- [ ] Анимация переходов между вариантами
- [ ] Поиск по узлам
- [ ] Редактирование координат в UI
