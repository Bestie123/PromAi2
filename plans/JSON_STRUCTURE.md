# Development Plan JSON Structure

## Описание для AI

Этот документ описывает структуру `development_plan.json` для работы нейросетей с планом разработки.

## Корневая структура

```json
{
  "version": "1.0.0",
  "project": "PromAi PKM",
  "description": "План разработки - Полный анализ всех уровней",
  "generated": "ISO 8601 timestamp",
  "source": "Источник данных",
  "summary": { /* Сводка */ },
  "legend": { /* Легенда приоритетов */ },
  "criticalPaths": [ /* Критические пути */ ],
  "levels": [ /* Уровни разработки */ ],
  "nodeMapping": { /* Маппинг решений */ },
  "compatibility": { /* Правила совместимости */ }
}
```

## 1. Метаданные (Meta)

```json
{
  "version": "1.0.0",           // Версия формата плана
  "project": "PromAi PKM",      // Название проекта
  "description": "string",      // Описание плана
  "generated": "2025-11-30T...", // ISO 8601 timestamp
  "source": "string"            // Источник данных
}
```

**Использование:** Проверка версии и актуальности плана.

## 2. Summary (Сводка)

```json
{
  "summary": {
    "totalDuration": "548-814 дней (18-27 месяцев)",
    "mvp": {
      "duration": "68-94 дня (10-13 недель)",
      "criticalPath": "50-65 дней",
      "parallelTasks": "18-29 дней"
    },
    "level1": "90-120 дней (3-4 месяца)",
    "level2": "120-180 дней (4-6 месяцев)",
    "level3": "90-150 дней (3-5 месяцев)",
    "level4": "180-270 дней (6-9 месяцев)"
  }
}
```

**Использование:** Быстрая оценка длительности проекта и каждого уровня.

## 3. Legend (Легенда приоритетов)

```json
{
  "legend": {
    "critical": "🔴 Критический путь - блокирует другие задачи",
    "parallel": "🟡 Параллельный - можно делать одновременно",
    "flexible": "🟢 Гибкий - можно отложить без блокировки"
  }
}
```

**Использование:** Понимание приоритетов задач.

## 4. Critical Paths (Критические пути)

```json
{
  "criticalPaths": [
    {
      "id": 1,
      "name": "Архитектура → CRDT",
      "duration": "35-50 дней",
      "risk": "МАКСИМАЛЬНЫЙ",
      "description": "Блокирует всё",
      "path": "№1 → №29 → №2 → №3 → №4 → №13 → №16 → №17"
    }
  ]
}
```

**Поля:**
- `id` - уникальный идентификатор пути
- `name` - название критического пути
- `duration` - длительность в днях
- `risk` - уровень риска (МАКСИМАЛЬНЫЙ, ВЫСОКИЙ)
- `description` - что блокирует
- `path` - последовательность критериев (№N)

**Использование:** Определение блокирующих зависимостей и рисков.

## 5. Levels (Уровни разработки)

### 5.1 Уровень 0 (MVP) - Особая структура

```json
{
  "levels": [
    {
      "id": 0,
      "name": "Несущие стены проекта (MVP)",
      "description": "29 критериев",
      "duration": "68-94 дня (10-13 недель)",
      "tracks": {
        "critical": { /* Критический путь */ },
        "parallel": { /* Параллельные задачи */ }
      }
    }
  ]
}
```

### 5.2 Track Structure (Критический/Параллельный)

```json
{
  "critical": {
    "name": "Критический путь",
    "duration": "50-65 дней",
    "weeks": "1-6",
    "criteria": [ /* Массив критериев */ ]
  }
}
```

### 5.3 Criterion Structure (Критерий)

```json
{
  "id": 1,
  "title": "Ядро отделено от UI и плагинов",
  "question": "Модульность движка?",
  "week": 1,
  "time": "2-3 дня",
  "priority": "critical",
  "requires": ["modular-arch"],
  "blocks": ["q2", "q3", "q29"],
  "solution": {
    "type": "selected",
    "name": "Модульная",
    "description": "Ядро легко отделяется",
    "pros": ["Тестирование, замена UI"],
    "cons": ["Сложность"]
  },
  "alternatives": [
    {
      "type": "rejected",
      "name": "Монолитная",
      "description": "Всё в одном модуле",
      "pros": ["Быстрый старт"],
      "cons": ["Спагетти-код"]
    }
  ]
}
```

**Поля критерия:**
- `id` - номер критерия (1-29)
- `title` - название критерия
- `question` - ключевой вопрос архитектуры
- `week` - неделя выполнения (null если параллельно)
- `time` - оценка времени
- `priority` - "critical" или "parallel"
- `requires` - массив зависимостей (ID решений)
- `blocks` - массив блокируемых вопросов (qN)
- `solution` - выбранное решение
- `alternatives` - альтернативные решения

**Solution/Alternative поля:**
- `type` - "selected", "rejected", "alternative"
- `name` - название решения
- `description` - описание
- `pros` - массив преимуществ
- `cons` - массив недостатков

### 5.4 Уровни 1-4 (Обычная структура)

```json
{
  "id": 1,
  "name": "Core UX и \"магия\" (после MVP, 3-6 месяцев)",
  "tasks": [
    {
      "title": "Live update без задержек",
      "requires": ["№13", "№16"],
      "time": "5-7 дней",
      "priority": "flexible"
    }
  ]
}
```

**Task поля:**
- `title` - название задачи
- `requires` - зависимости (№N или название задачи)
- `time` - оценка времени
- `priority` - всегда "flexible"

## 6. Node Mapping (Маппинг решений)

```json
{
  "nodeMapping": {
    "1": {
      "question": "q1",
      "selected": "modular-arch",
      "alternatives": ["monolith-arch"]
    },
    "13": {
      "question": "q13",
      "selected": "yjs-crdt",
      "alternatives": ["automerge-crdt"]
    }
  }
}
```

**Структура:**
- Ключ - номер критерия (строка)
- `question` - ID вопроса (qN)
- `selected` - ID выбранного решения
- `alternatives` - массив ID альтернативных решений

**Использование:** Связь между критериями и принятыми решениями.

## 7. Compatibility (Совместимость решений)

```json
{
  "compatibility": {
    "modular-arch,monolith-arch": "невозможно",
    "uuid-v4,uuid-v7": "переключать",
    "json-storage,hybrid-storage": "объединить"
  }
}
```

**Структура:**
- Ключ - пара решений через запятую
- Значение - тип совместимости:
  - `"невозможно"` - взаимоисключающие
  - `"переключать"` - выбор одного из двух
  - `"объединить"` - можно использовать вместе

**Использование:** Валидация комбинаций решений.

## Алгоритмы работы с JSON

### 1. Получить все критерии MVP

```javascript
const mvpCriteria = json.levels[0].tracks.critical.criteria
  .concat(json.levels[0].tracks.parallel.criteria);
```

### 2. Найти критерий по номеру

```javascript
function findCriterion(id) {
  const level0 = json.levels[0];
  return [...level0.tracks.critical.criteria, 
          ...level0.tracks.parallel.criteria]
    .find(c => c.id === id);
}
```

### 3. Получить зависимости критерия

```javascript
function getDependencies(criterionId) {
  const criterion = findCriterion(criterionId);
  return criterion.requires; // ["modular-arch", ...]
}
```

### 4. Проверить совместимость решений

```javascript
function checkCompatibility(solution1, solution2) {
  const key = `${solution1},${solution2}`;
  return json.compatibility[key] || 
         json.compatibility[`${solution2},${solution1}`];
}
```

### 5. Получить все задачи уровня

```javascript
function getLevelTasks(levelId) {
  const level = json.levels.find(l => l.id === levelId);
  return level.tasks || [];
}
```

### 6. Построить граф зависимостей

```javascript
function buildDependencyGraph() {
  const graph = {};
  
  // MVP критерии
  const mvp = json.levels[0];
  [...mvp.tracks.critical.criteria, 
   ...mvp.tracks.parallel.criteria].forEach(c => {
    graph[`№${c.id}`] = c.requires.map(r => `№${r}`);
  });
  
  // Остальные уровни
  json.levels.slice(1).forEach(level => {
    level.tasks.forEach(task => {
      graph[task.title] = task.requires;
    });
  });
  
  return graph;
}
```

### 7. Найти критический путь

```javascript
function getCriticalPath(pathId) {
  return json.criticalPaths.find(p => p.id === pathId);
}
```

### 8. Получить выбранное решение для критерия

```javascript
function getSelectedSolution(criterionId) {
  const mapping = json.nodeMapping[criterionId.toString()];
  return mapping ? mapping.selected : null;
}
```

## Примеры запросов

### Сколько времени займёт MVP?
```javascript
json.summary.mvp.duration // "68-94 дня (10-13 недель)"
```

### Какие критерии в критическом пути?
```javascript
json.levels[0].tracks.critical.criteria.map(c => ({
  id: c.id,
  title: c.title,
  time: c.time
}))
```

### Какие задачи зависят от CRDT (№13)?
```javascript
json.levels.slice(1).flatMap(level => 
  level.tasks.filter(t => t.requires.includes("№13"))
)
```

### Можно ли использовать JSON и SQLite вместе?
```javascript
checkCompatibility("json-storage", "sqlite-storage") 
// "переключать"

checkCompatibility("json-storage", "hybrid-storage")
// "объединить"
```

## Валидация структуры

### Обязательные поля корня:
- `version`, `project`, `summary`, `levels`, `nodeMapping`, `compatibility`

### Обязательные поля критерия:
- `id`, `title`, `question`, `time`, `priority`, `solution`

### Обязательные поля задачи:
- `title`, `requires`, `time`, `priority`

### Типы priority:
- Уровень 0: `"critical"`, `"parallel"`
- Уровни 1-4: `"flexible"`

### Типы solution.type:
- `"selected"` - выбранное решение
- `"rejected"` - отвергнутое решение
- `"alternative"` - возможная альтернатива (⚡)

## Расширение структуры

При добавлении новых полей:

1. **Обратная совместимость** - старые поля не удалять
2. **Версионирование** - увеличить `version`
3. **Документация** - обновить этот файл
4. **Валидация** - добавить проверки

## JSON Schema (опционально)

Для автоматической валидации можно создать JSON Schema:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["version", "project", "levels"],
  "properties": {
    "version": {"type": "string"},
    "project": {"type": "string"},
    "levels": {
      "type": "array",
      "items": {"$ref": "#/definitions/level"}
    }
  }
}
```

## Валидация структуры

### Автоматическая проверка

Используйте скрипт `validate_plan.js`:

```bash
node validate_plan.js
```

**Проверяет:**
- Наличие всех `requires` зависимостей
- Корректность `blocks` полей
- Соответствие критическим путям
- Консистентность графа зависимостей

**Результат:**
- ✅ All checks passed - структура корректна
- ❌ Validation failed - найдены ошибки/предупреждения

### Правила валидации

1. **Критический путь №1→№29→№2→№3→№4→№13→№16→№17**
   - Каждый критерий должен иметь `requires` от предыдущего
   - Каждый критерий должен блокировать следующий через `blocks`

2. **Параллельные задачи**
   - Могут иметь пустые `requires: []`
   - Должны иметь зависимости от критического пути если используют его результаты

3. **Blocks поле**
   - Содержит ID вопросов (qN), которые блокируются этим критерием
   - Используется для построения дерева решений

## Использование AI

### Для анализа плана:
1. Загрузить JSON
2. Использовать алгоритмы выше
3. Генерировать отчёты/визуализации

### Для обновления плана:
1. Прочитать текущую структуру
2. Сохранить `version` и метаданные
3. Обновить нужные секции
4. **Запустить валидацию** `node validate_plan.js`
5. Исправить ошибки если есть
6. Обновить `generated` timestamp

### Для генерации документации:
1. Извлечь данные из JSON
2. Форматировать в Markdown
3. Добавить визуализации (Mermaid, графы)
